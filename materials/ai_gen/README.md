# AI-generated drafts (promote accepted assets into web/public/ after review)

Outputs from `tools/asset_gen/` and Cursor GenerateImage should land here first:

- `img/` — posters, covers, icons
- `video/` — optional motion refs
- `clean/` — matted cutouts

Current invite share background: `img/cloudsteps-invite-poster-bg.png`
(promoted copy: `web/public/invite-poster-bg.png`).
